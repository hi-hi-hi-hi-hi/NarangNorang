<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.config.MiniroomMapper">

	<select id="selectAllItems" resultType="ItemDTO" parameterType="String">
	<![CDATA[
		select id, category, name, price
		from ITEM
		where category=#{category}

		]]>
	</select>

	<insert id="insertBuy" parameterType="MyItemDTO">
		Insert into MY_ITEM (id, member_ID, item_ID, wish)
		values (seq_My_Item.nextval, #{memberId}, #{itemId},#{wish} )

	</insert>

	<insert id="insertStyle" parameterType="MyItemDTO">
		Insert into MY_ROOM (id, member_ID, item_ID, wish)
		values (seq_My_Item.nextval, #{memberId}, #{itemId},#{wish} )

	</insert>

	<update id="wish" parameterType="int">
	<![CDATA[
		update MY_ITEM
		set wish = 1
		where item_Id = #{itemId}
		]]>
	</update>

	<update id="notWish" parameterType="MyItemDTO">
	<![CDATA[
		update MY_ITEM
		set wish = 0
		where item_Id = #{itemId}
		]]>
	</update>

	<select id="selectByMyItemId" resultType="MyItemDTO" parameterType="int">
	<![CDATA[
		select id, member_ID, item_ID, wish
		from MY_ITEM
		where item_Id=#{itemId}

		]]>
	</select>

	<select id="selectAllMyItems" resultType="MyItemDTO" parameterType="String">
	<![CDATA[
		select m.id, m.member_ID, m.item_ID, m.wish,i.category
		from MY_ITEM m
				 Inner Join ITEM i
							ON m.item_id = i.id
		where category=#{category}

		]]>
	</select>


</mapper>
